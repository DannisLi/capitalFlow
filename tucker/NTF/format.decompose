function [] = decompose%d(coreNway,lam)

load %s M;

M = tensor(M);
opts = [];
opts.maxit = 2000;
opts.lam = lam;
opts.tol = 2e-7;

[A,C,out] = ntds_fapg(M, coreNway, opts);

O = A{1};
D = A{2};
T = A{3};
C = C.data;


save %s O D T C;

